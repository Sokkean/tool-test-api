// node_modules/devframe/dist/constants.mjs
var DEVFRAME_DOCK_IMPORTS_FILENAME = "__client-imports.js";
var REMOTE_CONNECTION_KEY = "devframe-remote-connection";

// node_modules/devframe/dist/utils/events.mjs
function createEventEmitter() {
  const _listeners = {};
  function emit(event, ...args) {
    const callbacks = _listeners[event] || [];
    for (let i = 0, length = callbacks.length; i < length; i++) {
      const callback = callbacks[i];
      if (callback) callback(...args);
    }
  }
  function emitOnce(event, ...args) {
    emit(event, ...args);
    delete _listeners[event];
  }
  function on(event, cb) {
    (_listeners[event] ||= []).push(cb);
    return () => {
      _listeners[event] = _listeners[event]?.filter((i) => cb !== i);
    };
  }
  function once(event, cb) {
    const unsubscribe = on(event, ((...args) => {
      unsubscribe();
      return cb(...args);
    }));
    return unsubscribe;
  }
  return {
    _listeners,
    emit,
    emitOnce,
    on,
    once
  };
}

export {
  DEVFRAME_DOCK_IMPORTS_FILENAME,
  REMOTE_CONNECTION_KEY,
  createEventEmitter
};
//# sourceMappingURL=chunk-LXHOP6NO.js.map
